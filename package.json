{
  "name": "concise",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist",
    "package": "npm run clean && npm run build && zip -r concise.zip manifest.json dist/ icons/"
  },
  "dependencies": {
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.3"
  }
}
